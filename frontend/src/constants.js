// Global constants for frontend

export const REFRESH_INTERVAL_OPEN = 300000; // 5 minutes if market open (free api only has 500 calls a month)
export const REFRESH_INTERVAL_CLOSED = 0; // 0 if market closed
